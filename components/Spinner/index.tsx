// Import the stylesheet for the spinner component
import './style.scss';

// Define the Spinner component
export default function Spinner() {
	// Render a div with the class "spinner"
	return <div className="spinner"></div>;
}
